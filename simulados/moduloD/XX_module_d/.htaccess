RewriteEngine On

# Allow direct access to existing files (api.php, images, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all other requests to index.php
RewriteRule ^(.*)$ index.php [QSA,L]
